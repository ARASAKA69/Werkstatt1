- Fixing ausgabe system, when uploading StockID NA.jpg then upload that image as Nachbestellung 1/1 and also for NA (2) Nachbestellung 2/2 and so on inside the comment section in carol depend on how many images are added.

- When uploading StockID and StockID (2) simply Upload them as Ausgabe 1/1 , Ausgabe 2/2 and so on inside the comment section of carol depend on how many images are added.

- when uploading Retoure, like StockID Retoure.jpg simply upload them as Retoure 1/1 and Retoure 2/2 and so on inside the comment section in carol depend on how many images are added.

- Fixing Duplicate issue, when any of these uploaded with the same name, it shpuld skip them.

- add more infos inside log file, like how many are skipped, which are skipped and why, just to fin out later what was wrong.